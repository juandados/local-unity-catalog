# .env
# Copy this file to .env and update the values before running `docker-compose up`

# Unity Catalog settings (defaults shown)
UNITY_CATALOG_URI=http://server:8080    # URL for the "server" service defined in docker-compose
UNITY_CATALOG_TOKEN=                    # your Unity Catalog authentication token

# MLflow settings (uncomment to override defaults)
# MLFLOW_TRACKING_URI=http://0.0.0.0:5000
# MLFLOW_BACKEND_STORE_URI=/mlflow/mlruns
# MLFLOW_ARTIFACT_ROOT=/mlflow/mlruns